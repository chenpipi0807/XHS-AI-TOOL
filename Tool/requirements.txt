# ════════════════════════════════════════════════════════════════════
# XHS AI Tool — CLI 模式 Python 依赖清单
# 安装命令：pip install -r requirements.txt
# Python 版本要求：>= 3.10
# ════════════════════════════════════════════════════════════════════

# ── 环境变量 ──────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Anthropic SDK（Kimi API 使用 Anthropic 协议）─────────────────
anthropic>=0.25.0

# ── MCP SDK（SQLite MCP Server + XHS MCP Server）─────────────────
mcp>=1.0.0
starlette>=0.40.0
uvicorn>=0.30.0

# ── 定时任务（APScheduler，CLI 后台调度）─────────────────────────
APScheduler>=3.10.4

# ── 图片处理 ─────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Google GenAI SDK（Gemini 图像生成/编辑）─────────────────────
google-genai>=1.0.0

# ── 终端交互（多行输入，Shift+Enter 换行，粘贴友好）─────────────
prompt_toolkit>=3.0.0

# ── 日志 / 工具 ──────────────────────────────────────────────────
python-dateutil>=2.8.2

# ── HTTP 客户端（可选，requests 用于部分图片下载场景）───────────
requests>=2.31.0

# ════════════════════════════════════════════════════════════════════
# 开发依赖（可选）
# ════════════════════════════════════════════════════════════════════
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# black>=24.0.0
# ruff>=0.4.0
